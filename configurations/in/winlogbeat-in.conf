input {
  beats {
    port =>
  }
}

output {
  if [type] == "wineventlog" {
    rabbitmq {
      exchange => "winlogbeat"
      exchange_type => "direct"
      host => "x.x.x.x"
      port => 5671
      durable => true
      persistent => true
      ssl => true
      verify_ssl => true
    }
  }
}
