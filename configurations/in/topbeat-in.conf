input {
	beats {
		port =>
	}
}

output {
	if [type] == "process" or [type] == "system" or [type] == "filesystem" {
		rabbitmq {
			exchange => "topbeat"
			exchange_type => "direct"
			host => "x.x.x.x"
			port => 5671
      durable => true
      persistent => true
      ssl => true
      verify_ssl => true
			user => ""
			password => ""
		}
	}
}
